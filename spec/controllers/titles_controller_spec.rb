require 'spec_helper'

describe TitlesController do
	# it 'should display an error for a title not found' do
		# get :show, id: '2202-nothing-or-something'
		# expect(response).to redirect_to titles_path
		# expect(flash[:alert]).to have_content 'No, no such title.'
	# end	
end
